export enum ButtonType {
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
  DANGER = 'danger',
}

export enum ButtonState {
  ACTIVE = 'active',
  DISABLED = 'disabled',
  LOADING = 'loading'
}

export enum SizeType {
  DEFAULT = 'default',
  MAX_PERCENT = '100%'
}
